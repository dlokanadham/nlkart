{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_ARG_file_path\" | grep -qE '\\.(jsx?|tsx?|css|json)$'; then npx prettier --write \"$CLAUDE_TOOL_ARG_file_path\" 2>&1; exit 0; else exit 0; fi"
          }
        ]
      }
    ]
  }
}
